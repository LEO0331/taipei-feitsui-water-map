import { mkdir, writeFile } from 'node:fs/promises'; import path from 'node:path';
const url='https://data.taipei/api/frontstage/tpeod/dataset/resource.download?rid=db100213-001f-4eb1-a2b3-008c9928a201';const dir=path.join(process.cwd(),'data/raw/river-ecology');await mkdir(dir,{recursive:true});const response=await fetch(url);if(!response.ok)throw new Error(`Download failed: ${response.status}`);await writeFile(path.join(dir,'benthic-species.csv'),Buffer.from(await response.arrayBuffer()));await writeFile(path.join(dir,'source-metadata.json'),JSON.stringify({datasetId:'4cb2cb61-1b3f-4c0f-894b-353266b8a06b',url,downloadedAt:new Date().toISOString()},null,2));console.log('Fetched river ecology CSV.');
